#include"feature_option.h"
#include<stdio.h>
 void prepare_opts(featParams *param,pathParam *resPath)
{

     param->featDim = 2*(param->PCADIM+2)*param->numSpatialX*param->numSpatialY*param->G;

    // resPath->lexPath = "lex.txt";
     //resPath->modelPath="Model.txt";

     //sprintf('')

   }
